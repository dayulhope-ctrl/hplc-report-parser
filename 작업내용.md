# LCMS 분석앱 작업 내용

## 프로젝트 개요

- **앱 URL**: https://hplc-report-parser-ew2uvmgaovaydmwu4a2cgn.streamlit.app
- **GitHub**: https://github.com/dayulhope-ctrl/hplc-report-parser
- **로컬 경로**: `C:\Users\user\Downloads\LCMS분析앱` (git repo)
- **플랫폼**: Streamlit Cloud (자동 배포)

---

## 제품 구성

| 버튼 | 제품명 | 시험 항목 |
|------|--------|-----------|
| 💊 환제 | 우황청심원 환제 | 확인(ID), 함량(AS) |
| 🧪 현탁제 | 우황청심원 현탁제 | 확인(ID), 함량(AS) |
| 💫 공진단 | 공진단 | 확인(ID), 함량(AS) |
| 🔬 파워라센 아미노산 | 파워라센 | 함량(AS) |

---

## 공진단 추가 작업 (주요 작업)

### 1. CSV 파일 구조

#### 확인(ID) 데이터 - S/N 값 사용
| 파일명 | 내용 |
|--------|------|
| `ID_SST.csv` | 표준액 6런 (STD-1~STD-6), RT + S/N |
| `ID_SP_A.csv` | 검액 A (Ginsenoside Rg1, Rb1, Cinnamic acid-d6) |
| `ID_SP_B.csv` | 검액 B (Morroniside, Loganin, Nodakenin, Decursin, Decursin Angelate) |

#### 함량(AS) 데이터 - Area 값 사용
| 파일명 | 내용 |
|--------|------|
| `AS_STD.csv` | 표준액 6런, RT + Area |
| `AS_SP_A.csv` | 검액 A (Ginsenoside Rb1) |
| `AS_SP_B.csv` | 검액 B (Loganin, Decursin, Decursin Angelate) |

### 2. 생성 파일 목록

```
core/
├── gj_csv_parser.py         # CSV 파싱 (csv_utils 범용 엔진 사용)
├── gj_id_excel_writer.py    # 확인시험 엑셀 생성
└── gj_as_excel_writer.py    # 함량시험 엑셀 생성
```

### 3. 확인시험 (gj_id_excel_writer.py)

#### 화합물 구성 및 transition 순서

| 화합물 | Transition 1 (Quantifier) | Transition 2 | Transition 3 |
|--------|--------------------------|--------------|--------------|
| Morroniside | 465.3 → 243.2 | 465.3 → 141.1 | 465.3 → 155.0 |
| Loganin | 449.2 → 227.0 | 449.2 → 101.0 | 449.2 → 127.0 |
| Nodakenin | 409.2 → 186.9 | 409.2 → 229.0 | 409.2 → 247.2 |
| Ginsenoside Rg1 | 859.6 → 637.5 | 859.6 → 475.5 | 859.6 → 799.7 |
| Ginsenoside Rb1 | 1107.6 → 945.7 | 1107.6 → 178.8 | 1107.6 → 221.2 |
| Decursin | 329.1 → 247.2 | 329.1 → 115.0 | 329.1 → 128.0 |
| Decursin angelate | 329.1 → 247.2 | 329.1 → 115.0 | 329.1 → 128.0 |

#### 섹션 구성

**● 시스템적합성**
- 그룹1: Morroniside, Loganin, Nodakenin
- 그룹2: Ginsenoside Rg1, Ginsenoside Rb1, Decursin
- 데이터: STD 6런 × 각 transition별 RT 값 (노란색 셀)
- 통계: 평균, 표준편차, 유지시간 RSD% (2.0% 이하)

**● 결과 확인**
- 그룹1: Ginsenoside Rg1, Ginsenoside Rb1, Morroniside
- 그룹2: Loganin, Nodakenin, Decursin
- 그룹3: Decursin angelate
- 데이터: SP 각 run × 각 transition별 S/N 값 (노란색 셀)
- A열에 샘플명 표시 (예: `S/N비 / 25006_B-1`)
- 결과: S/N ≥ 10이면 "검출", 미만이면 "미검출"

#### 파싱 방식
- `prefer="sn"` 사용 → S/N 값을 "area" 키에 저장
- 각 transition을 개별 화합물로 파싱 후 `_lookup_trans(parsed, comp_name, transition)`으로 조회
- SST 파일명 인식: 파일명에 `STD` 또는 `SST` 포함 시 표준액으로 분류

### 4. 함량시험 (gj_as_excel_writer.py)

#### 대상 화합물

| 화합물 | SP 파일 | 특이사항 |
|--------|---------|---------|
| Loganin | SP-B | - |
| Ginsenoside Rb1 | SP-A | - |
| Decursin | SP-B | **Decursin Angelate Area 합산** |

#### Decursin Area 합산 처리
- `Decursin Area + Decursin Angelate Area` 합산하여 입력
- 예) SP-B: 305,580 + 179,651 = **485,231**
- STD, SP 섹션 모두 적용

#### 엑셀 구조 (화합물별 시트)
각 시트: `표준액 정보` → `검액 정보` → `IS 검액 정보`

```
[표준액 정보 - Loganin]
구분 | Loganin RT | Loganin Area | IS RT | IS Area | STD/IS 비율
Value | ... (6런) ...
평균 / 표준편차 / %RSD

[검액 정보 - Loganin]
구분 | 25006_B (lot명)
Area | ... (3런 최대) ...
평균 / 표준편차 / %RSD

[IS 검액 정보 - Cinnamic acid-d6]
동일 구조
```

---

## 파일 자동 분류 규칙

### 공진단 CSV 파일명 → 분류

| 파일명 패턴 | 분류 |
|------------|------|
| `*STD*`, `*SST*` | 표준액 (std) |
| `*SP_A*`, `*SPA*` | 검액 A |
| `*SP_B*`, `*SPB*` | 검액 B |
| 그 외 | 표준액으로 처리 |

### csv_utils.classify() 행 분류

| 샘플명 패턴 | 분류 |
|------------|------|
| `STABILITY` 포함 | stability |
| `STD` 포함 (STD-1 등) | std |
| 그 외 | sp |

---

## 스타일 가이드

| 항목 | 값 |
|------|-----|
| 노란색 (입력 셀) | `FFFFE1` |
| 파란색 (헤더) | `BDD7EE` |
| 회색 (통계) | `D9D9D9` |
| 타이틀 배경 | `2E75B6` (흰 글씨) |
| 폰트 | 맑은 고딕 |
| 테두리 | 전체 thin (굵은 테두리 없음) |

---

## 주요 이슈 및 해결

### 1. Unicode 경로 이중 디렉토리 문제
- **문제**: 동일해 보이는 두 폴더 존재
  - `LCMS분析앱` (U+6790 中文 析) → git 없음
  - `LCMS분析앱` (U+C11D 한글 석) → **git repo** (실제 사용)
- **해결**: Python으로 `.git` 디렉토리 유무 확인 후 정확한 경로 사용
  ```python
  for item in os.listdir(base):
      if item.startswith("LCMS") and item.endswith("앱"):
          if os.path.isdir(os.path.join(base, item, ".git")):
              repo = os.path.join(base, item)
  ```
- **주의**: `Write`, `Edit` 도구는 항상 non-git 경로에 저장 → `shutil.copy2()`로 git repo에 복사 필요

### 2. 함량 엑셀 템플릿 OneDrive Stub 문제
- **문제**: `gj_as_template.xlsx` 파일이 OneDrive 스텁 (`<DOC` 서명) → `BadZipFile` 오류
- **해결**: 템플릿 의존성 제거, `openpyxl`로 처음부터 엑셀 생성

### 3. streamlit_app.py 빈 파일 문제
- **문제**: 수정 스크립트 실행 중 파일 내용이 3바이트(BOM만)로 덮어쓰임 → 앱 블랙 스크린
- **해결**: `git show <commit>:streamlit_app.py`로 이전 커밋에서 복구

### 4. __pycache__ 로딩 문제
- **문제**: `.py` 파일 수정 후에도 이전 `.pyc` 캐시 로드
- **해결**: 테스트 전 `core/__pycache__/gj_*.cpython-*.pyc` 삭제

---

## 배포 프로세스

```bash
# 1. 파일 수정 (Write/Edit 도구 → non-git 경로)
# 2. git repo로 복사
shutil.copy2(src_nogit, dst_git)
# 3. __pycache__ 삭제
# 4. 로컬 테스트
python test_gj_id2.py
python test_gj_as.xlsx
# 5. git add, commit, push
git add core/gj_id_excel_writer.py
git commit -m "..."
git push origin main
# → Streamlit Cloud 자동 재배포
```

---

## 커밋 히스토리 (공진단 관련)

| 커밋 | 내용 |
|------|------|
| `9da3e7e` | 확인 엑셀: 검체명 표시(S/N비 행에 샘플 이름 추가), 굵은 테두리 제거 유지 |
| `6fff347` | 확인 엑셀: 구분 아래 굵은 테두리 제거 |
| `1629d75` | streamlit_app.py 복구 및 공진단 확인 S/N 파싱 fix |
| `9bb808c` | 공진단 확인: S/N 파싱 지원, transition별 RT/S/N 개별 입력 |
| `544e5b7` | 공진단 CSV 업로드 UI: SP_C 항목 제거 |
| `ffa8090` | Decursin 함량: Decursin Angelate Area 합산 처리 |
| `06cb6d0` | 공진단 추가: 확인/함량 CSV 파서 및 엑셀 라이터, streamlit UI 4버튼 업데이트 |
